<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.code.multidexinstall">

    <application
        android:name=".MineApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoadMultidexActivity"
            android:process=":multidex_load" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/ActivityTheme">

        </activity>

        <receiver android:name=".MultidexBroadCastReceiver">
            <intent-filter>
                <action android:name="com.code.multidex" />
            </intent-filter>
        </receiver>
    </application>

</manifest>